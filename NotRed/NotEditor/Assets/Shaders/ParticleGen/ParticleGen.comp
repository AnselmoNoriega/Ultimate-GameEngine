#version 450

struct Particle {
    vec4 position;
    vec4 velocity;
    float mass;
    int id;
};

layout(set = 0, binding = 8) buffer ParticleData {
    Particle data[200];
};

void main() 
{
    uint id = gl_GlobalInvocationID.x;
    if (id < 200) // Ensure we're within bounds
    {
        data[id].id = 5; // Set the ID for the current particle
    }
}