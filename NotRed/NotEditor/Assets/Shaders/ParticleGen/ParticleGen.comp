#version 450

struct Particle {
    vec4 position;
    vec4 velocity;
    float mass;
    int id;
};

layout(std430, binding = 8) buffer MyDataBuffer {
    Particle data[200];
};

void main() 
{
    for(int i = 0; i < 200; ++i)
    {
        data[i].id = 2;
    }
}